# How to start the project
```
1. install nodejs, ex: v16.17.1
2. run => node --version, in terminal
it works when returning => v16.17.1
3. npm install -g @vue/cli
4. vue create my-project
5. Choose what you want, ex: vue-config-1 
```